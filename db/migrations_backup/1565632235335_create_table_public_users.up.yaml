- args:
    sql: CREATE TABLE "public"."users"("id" serial NOT NULL, "nickname" text, "name"
      text NOT NULL, "email" text, "picture" text, "created_at" timestamp NOT NULL,
      "updated_at" timestamp NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: users
    schema: public
  type: add_existing_table_or_view
