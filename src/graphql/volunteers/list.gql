query volunteerList (
  $offset: Int!,
  $limit: Int!,
  $sort: [volunteer_list_order_by!]
) {
  volunteer_list (
    order_by: $sort,
    limit: $limit,
    offset: $offset
  ) {
    id
    first_name
    last_name
    points
    shows
  }
}
